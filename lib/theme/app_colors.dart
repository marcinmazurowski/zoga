import 'package:flutter/material.dart';

/// The single place defining the app's theme colors.
/// Changing the values here changes the look of the whole app.
class AppColors {
  AppColors._();

  static const Color background = Color(0xFFFAFAFA);
  static const Color primary = Color(0xFF00695C);
  static const Color secondary = Color.fromARGB(255, 0, 147, 132);
  static const Color textPrimary = Color(0xDD000000);
  static const Color textSecondary = Color(0xFF757575);
}
